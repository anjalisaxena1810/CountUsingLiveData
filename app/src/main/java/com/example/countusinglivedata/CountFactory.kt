package com.example.countusinglivedata

import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider

class CountFactory{
// (private val inputvalue : Int):ViewModelProvider.Factory {
//    override fun <T : ViewModel> create(modelClass: Class<T>): T {
//        if (modelClass.isAssignableFrom(CountViewModel::class.java)){
//            return CountViewModel(inputvalue) as T
//        }
//        throw IllegalArgumentException("Unknown Error")
//    }
}
